<th>
  <%= link_to params.to_unsafe_h.merge(:order => new_order) do %>

    <%= human_label(model_name, attr_name) %>

    <i class="glyphicon glyphicon-chevron-<%= direction %> <%= 'muted' unless current_sort_field.to_s == attr_name.to_s %>"></i>
  <%- end -%>
</th>
