- query_params = request.query_parameters
- if !query_params or query_params.empty?
  This request did not contain any parameters.
- else
  %table
    - request.query_parameters.each do |key, value|
      %tr
        %td= key
        %td= value


