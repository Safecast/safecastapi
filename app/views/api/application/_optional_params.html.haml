- if !@optional_params or @optional_params.empty?
  This endpoint does not support any optional parameters.
- else
  %table
    - @optional_params.each do |key, value|
      %tr
        %td= key
        %td= value



