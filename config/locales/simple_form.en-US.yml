en-US:
  helpers:
    submit:
      bgeigie_import:
        create: "Upload"
        update: "Save"
      device:
        create: "Save"
        update: "Save"
  simple_form:
    creating: "Creating..."
    updating: 'Updating...'
    buttons:
      or: 'or'
      cancel: 'cancel'
    "yes": 'Yes'
    "no": 'No'
    required:
      text: 'required'
      mark: '*'
      # You can uncomment the line below if you need to overwrite the whole required html.
      # When using html, text and mark won't be used.
      # html: '<abbr title="required">*</abbr>'
    error_notification:
      default_message: "Please review the problems below:"
    # Labels and hints examples
    labels:
    #   defaults:
    #     password: 'Password'
      bgeigie_import:
        source: Upload a File
        name: Title (optional)
        description: Description (optional)
        credits_as_string: "Credits (comma-separated, optional)"
        cities_as_string: "Cities (comma seperated list, optional)"
        height: "Sensor Height (metres, optional)"
        orientation: "Sensor Orientation (optional)"
    #       email: 'E-mail to sign in.'
    #     edit:
    #       email: 'E-mail.'
    # hints:
    #   defaults:
    #     username: 'User name to sign in.'
    #     password: 'No special characters, please.'

